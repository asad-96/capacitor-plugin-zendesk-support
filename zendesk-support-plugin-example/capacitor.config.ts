import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'zendesk-support-plugin-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
